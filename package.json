{
  "name": "public-operations-observatory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:root": "tsc -p tsconfig.build.json",
    "build:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run build:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run build",
    "collect:github": "tsx src/cli.ts collect github",
    "backfill:github-history": "tsx src/cli.ts backfill github-history",
    "normalize:github": "tsx src/cli.ts normalize github",
    "briefing:weekly": "tsx src/cli.ts briefing weekly",
    "db:migrate": "tsx src/cli.ts db migrate",
    "maintenance:retention": "tsx src/cli.ts maintenance retention",
    "maintenance:verify-restore": "bash scripts/verify-backup-restore.sh",
    "db:verify-read-role": "psql \"${DATABASE_URL:?DATABASE_URL is required}\" -v ON_ERROR_STOP=1 -f scripts/verify-read-model-role.sql",
    "format": "prettier --write .",
    "format:root": "prettier --write .",
    "format:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run format:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run format",
    "format:check": "prettier --check .",
    "format:check:root": "prettier --check .",
    "format:check:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run format:check:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run format:check",
    "lint": "eslint .",
    "lint:root": "eslint .",
    "lint:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run lint:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run lint",
    "test": "vitest run",
    "test:root": "vitest run",
    "test:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run test:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run test",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "typecheck:root": "tsc --noEmit",
    "typecheck:workspace": "corepack pnpm --recursive --include-workspace-root --if-present run typecheck:root && corepack pnpm --recursive --filter './apps/**' --filter './packages/**' --if-present run typecheck"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.18.0",
    "@types/pg": "^8.15.5",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0",
    "vitest": "^3.2.4"
  },
  "pnpm": {
    "overrides": {
      "postcss": ">=8.5.18",
      "sharp": ">=0.35.0"
    }
  }
}
